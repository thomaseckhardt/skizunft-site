---
interface Props {
  date: Date
}

const { date, ...props } = Astro.props
---

<time datetime={date.toISOString()} {...props}>
  {
    date.toLocaleDateString('de-DE', {
      year: 'numeric',
      month: 'short',
      day: 'numeric',
    })
  }
</time>
